{
  "name": "airhorn-mono",
  "version": "5.0.4",
  "description": "Cloud Notification Library",
  "repository": "https://github.com/jaredwray/airhorn.git",
  "author": "Jared Wray <me@jaredwray.com>",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm -r test",
    "test:ci": "pnpm -r test:ci",
    "website:build": "pnpm docula build",
    "website:serve": "pnpm docula serve",
    "version:sync": "tsx scripts/version-sync.ts",
    "publish:airhorn": "pnpm recursive --filter 'airhorn' run publish",
    "publish:aws": "pnpm recursive --filter '@airhornjs/aws' run publish",
    "publish:azure": "pnpm recursive --filter '@airhornjs/azure' run publish",
    "publish:twilio": "pnpm recursive --filter '@airhornjs/twilio' run publish",
    "build": "pnpm -r build",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.3",
    "@types/node": "^24.3.1",
    "@vitest/coverage-v8": "^3.2.4",
    "docula": "^0.20.0",
    "tsup": "^8.5.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
